version: "3"

networks:
  backend:

services:
  db:
    image: "mcr.microsoft.com/mssql/server:2017-latest"
    environment:
      SA_PASSWORD: "OpenSim123"
      ACCEPT_EULA: "Y"
      MSSQL_PID: "Enterprise"
    networks:
      - backend
    ports:
      - "1433:1433"
         
  web:
    build: .
    depends_on:
      - db
    networks:
      - backend
    ports:
      - 8000:80
    